@model FlyAway.Models.Rezervacija

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Rezervacija</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="IdPutnika" class="control-label"></label>
                <select asp-for="IdPutnika" class="form-control" asp-items="ViewBag.IdPutnika"></select>
                <span asp-validation-for="IdPutnika" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdLeta" class="control-label"></label>
                <select asp-for="IdLeta" class="form-control" asp-items="ViewBag.IdLeta"></select>
                <span asp-validation-for="IdLeta" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
