@model IEnumerable<FlyAway.Models.Karta>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PutnikId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LetId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BrojKreditneKartice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NacinPlacanja)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PutnikId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LetId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BrojKreditneKartice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NacinPlacanja)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
